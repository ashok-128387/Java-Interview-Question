import java.io.*;
class reversenumber{
    public static void main(String arg[]){
        int n=123,rem;
        int reverse=0;
        while(n!=0){
            rem=n%10;
            reverse=(reverse*10)+rem;
            n=n/10;
            
        }
        System.out.println(reverse);
    }
}
